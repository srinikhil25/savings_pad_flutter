org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental compiler keeps memory-mapped cache files open, and on
# Windows it intermittently cannot release them, failing the build with
# "Could not close incremental caches in ...\caches-jvm\jvm\kotlin".
# Turning it off costs a few seconds per build and removes the failure.
kotlin.incremental=false
